name: Backend CD

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - 'k8s/**'
      - '.github/workflows/reusable-cd.yml'
  workflow_dispatch:

jobs:
  call-reusable-cd:
    uses: ./.github/workflows/reusable-cd.yml
    with:
      service: backend
    secrets: inherit