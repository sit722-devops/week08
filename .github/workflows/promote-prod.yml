name: Promote to Production (Simulated)

on:
  workflow_dispatch:
    inputs:
      product_tag: { description: 'product tag', required: true, default: 'latest' }
      order_tag:   { description: 'order tag', required: true, default: 'latest' }

jobs:
  gate:
    runs-on: ubuntu-latest
    environment: Production  # create this Environment in repo Settings
    steps:
      - name: Approved – simulate promote
        run: |
          echo "Production approved."
          echo "Skipping real AKS deploy (resources deleted)."
          echo "Would promote: product:${{ inputs.product_tag }}, order:${{ inputs.order_tag }}"
