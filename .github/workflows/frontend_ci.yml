name: Frontend CI - Build & Push Image

on:
  workflow_call: {}                     # called by the orchestrator
  pull_request:                         # PR checks (no push)
    branches: [ main, dev, develop ]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend_ci.yml'

env:
  ACR_LOGIN_SERVER: ${{ secrets.AZURE_CONTAINER_REGISTRY }}

jobs:
  build_and_push_frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # PRs: verify it builds, but don't push
      - name: Build Frontend (PR check only)
        if: ${{ github.event_name == 'pull_request' }}
        run: docker build -t frontend-ci-check ./frontend/

      # Orchestrated main: build & push :latest (and :SHORT_SHA)
      - name: Azure Login
        if: ${{ github.event_name == 'workflow_call' }}
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Derive ACR name and SHORT_SHA
        if: ${{ github.event_name == 'workflow_call' }}
        run: |
          echo "ACR_NAME=${ACR_LOGIN_SERVER%%.*}" >> $GITHUB_ENV
          echo "SHORT_SHA=${GITHUB_SHA::7}" >> $GITHUB_ENV

      - name: Login to Azure Container Registry
        if: ${{ github.event_name == 'workflow_call' }}
        run: az acr login --name ${{ env.ACR_NAME }}

      # NEW: tag and push :latest (and keep :SHORT_SHA)
      - name: Build & Push frontend (:latest and :SHORT_SHA)
        if: ${{ github.event_name == 'workflow_call' }}
        run: |
          docker build \
            -t ${{ env.ACR_LOGIN_SERVER }}/frontend:latest \
            -t ${{ env.ACR_LOGIN_SERVER }}/frontend:${SHORT_SHA} \
            ./frontend/
          docker push ${{ env.ACR_LOGIN_SERVER }}/frontend:latest
          docker push ${{ env.ACR_LOGIN_SERVER }}/frontend:${SHORT_SHA}

      - name: Logout Azure
        if: ${{ always() && github.event_name == 'workflow_call' }}
        run: az logout
