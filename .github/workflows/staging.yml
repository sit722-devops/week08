name: Deploy to Staging (Simulated)

on:
  workflow_dispatch:
    inputs:
      product_tag: { description: 'product tag', required: true, default: 'latest' }
      order_tag:   { description: 'order tag', required: true, default: 'latest' }

jobs:
  gate:
    runs-on: ubuntu-latest
    environment: staging   # create this Environment in repo Settings
    steps:
      - name: Approved – simulate deploy
        run: |
          echo "Staging approved."
          echo "Skipping real AKS deploy (resources deleted)."
          echo "Would deploy: product:${{ inputs.product_tag }}, order:${{ inputs.order_tag }}"
